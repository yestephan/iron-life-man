'use client';

// Providers component - currently no providers needed since we're using Supabase Auth directly
// This file is kept for potential future providers (e.g., theme, i18n, etc.)

export function Providers({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
